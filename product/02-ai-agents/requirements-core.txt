# aks-core bağımlılıkları — pyproject.toml ile birebir aynı olmalı (U12: eski
# şap>=0.44/FastAPI pinleri kaldırıldı; Django backend'e geçildi, ayrı API
# stack'i gerekmiyor — bkz. 04-backend/requirements.txt).
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.0
shap>=0.52   # 0.49 ve altı xgboost>=3 base_score formatını okuyamıyor
joblib>=1.3
numpy>=1.26
pytest>=8.0
