# AKS Django backend
Django>=5.0,<6
djangorestframework>=3.15
django-cors-headers>=4.3
dj-database-url>=2.1
django-redis>=5.4
psycopg2-binary>=2.9
python-dotenv>=1.0
gunicorn>=21.2
whitenoise>=6.6  # §7.15 — tek servis dağıtımı: statik dosyalar + derlenmiş React arayüzü
anthropic>=0.40  # §3b Phase 7/7.5 — danisman_llm.py tool-calling; anahtar yoksa fallback
google-genai>=1.0  # §3b Phase 7/7.10 — danisman_llm.py Gemini tool-calling yolu; anahtar yoksa fallback

# AI çekirdeği ayrı paket olarak kurulur (editable):
#   pip install -e ../02-ai-agents
# (o paketin belge/ modülü pdfplumber/pandas/openpyxl'e bağımlı — bkz.
#  02-ai-agents/pyproject.toml)
